---

---

<div class="flex flex-row justify-between items-start px-3 gap-2">
    <form action="/search" method="GET" class="max-w-md mx-auto">
        <div class="relative">
          <input 
            type="text"
            name="q"
            placeholder="Search..."
        class="rounded-full border border-gray-300 px-4 h-14 leading-14 w-full text-gray-300 bg-white font-semibold"
    >
        <button
            type="submit"
            class="absolute right-2 top-1/2 transform -translate-y-1/2 text-gray-500 hover:text-blue-600"
        ></button>
        </div>
    </form>
    <a
        class="rounded-full border border-gray-300 px-4 h-14 leading-14 text-red-600 bg-white font-semibold"
        >sdfjks</a
    >
</div>
